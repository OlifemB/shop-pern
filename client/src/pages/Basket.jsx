import React from 'react';
import Layout from "../components/Layout";

const Basket = () => {
    return (
        <Layout>
            BASKET
        </Layout>
    );
};

export default Basket;